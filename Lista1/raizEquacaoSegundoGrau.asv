function [x1, x2] = raizEquacaoSegundoGrau(a, b, c)
    delta = b^2 - 4*a*c;
    
    if(delta > 0)
        x1 = (-b + sqrt(delta))/2*a;
        x2 = (-b - sqrt(delta))/2*a;
    elseif(delta == 0)
        x1 =(-b + sqrt(delta))/2*a;
        x2 = x1;
    else
        fprintf("\n%.2fX²+
       